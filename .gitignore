.encore
encore.gen.go
encore.gen.cue
/.encore
node_modules
/encore.gen
*.tsbuildinfo

# OS
.DS_Store

# Env / secrets
.env
.env.*
!.env.example

# Local data
*.db
*.db-journal
/data
/.dashboard-data

# Local-only scratch space (e.g. real-data samples pulled from a live
# deployment for analysis) -- never meant to be committed, this repo is
# public.
/tmp/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

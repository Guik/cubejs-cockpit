.encore
encore.gen.go
encore.gen.cue
/.encore
node_modules
/encore.gen
*.tsbuildinfo

# OS
.DS_Store

# Env / secrets
.env
.env.*
!.env.example

# Local data
*.db
*.db-journal
/data
/.dashboard-data

# Your own Cube deployment, if running the full stack via docker-compose.yml
# (see the README's "Running the full stack" section) -- specific to your
# data model, never meant to live in this repo.
/cube.js
/schema
/.cubestore

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

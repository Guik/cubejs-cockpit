.encore
encore.gen.go
encore.gen.cue
/.encore
node_modules
/encore.gen
*.tsbuildinfo

# OS
.DS_Store

# Env / secrets
.env
.env.*
!.env.example

# Local data
*.db
*.db-journal
/data
/.dashboard-data

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
